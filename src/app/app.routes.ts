import { Routes } from '@angular/router';
import { BudgetGenerationComponent } from './main/budget-generation/budget-generation.component';

export const routes: Routes = [
  { path: '', component: BudgetGenerationComponent }
];
